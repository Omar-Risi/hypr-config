-- Programs used across keybinds/autostart on both machines.
-- Change once here instead of hunting through binds.lua.
return {
    terminal    = "wezterm",
    browser     = "brave",
    fileManager = "dolphin",
    menu        = "hyprlauncher",
}
